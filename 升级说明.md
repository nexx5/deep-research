# 升级说明：V0.1 → V0.2

> 本文档说明从 V0.1 升级到 V0.2 的方法与注意事项。
> 变更详情见 [CHANGELOG.md](CHANGELOG.md)。

## 升级步骤

### 1. 拉取新版本

```bash
git pull origin main
```

或将运行包三件套（`AGENTS.md`、`opencode.json`、`.opencode/`）整体替换为 V0.2 版本。

### 2. 确认嵌入 API 配置（无变化，可跳过）

V0.2 **没有改动配置机制**，沿用 V0.1 的 `.env` 配置方式：

- 复制 `.env.example` 为 `.env`，填写 `EMBED_BASE_URL` / `EMBED_API_KEY` / `EMBED_MODEL`。
- 兼容兜底：环境变量（EMBED_*/QMD_OPENAI_*）> 项目 `knowledge-pack/config.json` 的 `embed` 段 > 脚本默认值。

### 3. 存量项目无需迁移

V0.2 **数据格式不变**：`claims.jsonl` / `relations.jsonl` / SQLite schema 均未动。已有知识包可直接继续使用，无需重建索引。

## 新特性速览（V0.2）

### 批量 ingest（写入层提速 8-40 倍）

知识管理员 consolidation 的写入层从"逐条 ingest（每条触发 4 个全量子进程）"改为"一次批量 ingest（全链路只跑 1 次）"。实测小库约 8.7 倍、大库 100 条预估 20-40 倍。

```bash
# 批量写入（推荐）
python "<skill目录>/scripts/knowledge-ingest.py" --project-path "..." --claims-file claims.json
```

### per-claim 关系

批量模式下每条断言可带 `_relation`（extends/coexist/opposing/...）或 `_merge_into` 内部字段，pop 后不入库：

```json
[
  {"statement":"...","_relation":"extends:CL00001"},
  {"statement":"...","_relation":"opposing:CL00002"},
  {"statement":"...","_merge_into":"CL00003"}
]
```

### 知识管理员流程升级

- Step1 重写为"串行判断 + 批量写"：判断产出带 `_search_evidence`（query / candidates_returned / basis），"先查后写"从承诺变为可验证数据。
- 批量 `_relation: opposing:CLxxxx` 自动标双方 contested + 互加 opposing + 写 relations.jsonl，仲裁统一到 Step2。
- 失败兜底重提闭环：写入后必须解析 `results[].error`，失败条目修正后单独重提，**禁止整批重跑**。

## 向后兼容

| 旧用法 | V0.2 状态 |
|---|---|
| 单条 `--claim '{...}'` | ✅ 保留 |
| 函数级 `--relation extends:CLxxxx` | ✅ 保留 |
| 函数级 `--merge-into CLxxxx` | ✅ 保留 |
| `.env` 嵌入 API 配置 | ✅ 保留（机制未变） |
| 存量知识包数据 | ✅ 无需迁移 |

## 已知限制（沿用 V0.1）

- `多源搜索`、`html-report` 为全局 skill 单独部署，不随运行包发布；缺失时网络采集/HTML 渲染受限。
- 报告 agent 只生成自然 Markdown，HTML 渲染由 html-report skill 负责。
- 社区平台抓取能力仍在建设中：搜不到全文时记录为线索/死胡同，不静默忽略。

## 遇到问题？

- 确认版本：`git describe --tags` 应返回 `v0.2`。
- 检查脚本语法：`python -m py_compile .opencode/skills/zhishibao/scripts/knowledge-ingest.py`。
- 提出 Issue 或 PR（见 [README.md](README.md#贡献)）。
