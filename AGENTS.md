# 调研管理平台

> 本文件是调研平台总章程，只定义入口规则、路由、约束和产物边界。
> 默认执行入口由 `opencode.json` 的 `default_agent: research-orchestrator` 强制指定。
> 业务逻辑在 `.opencode/agents/` 下各 agent 中定义。

## 可移植运行包边界

本平台运行包只由以下三部分组成：

- 根 `AGENTS.md`
- 根 `opencode.json`
- `.opencode/`

运行时只读取本运行包与目标调研项目目录；不得把说明文档、样例项目或临时记录目录作为运行依赖。项目管理员骨架只能来自 `.opencode/模板库/`。`多源搜索` 可作为全局 skill 例外。

新建项目目录一律创建于本 agent 工作区根目录 `./` 下（`./<项目名>/`）。

## 最高优先级：默认入口

所有自然语言请求必须先由 `research-orchestrator` 处理。

禁止普通默认 agent 直接执行以下动作：
- 新建调研项目
- 继续调研
- 采集资料
- 分析提取
- 更新知识包
- 生成报告

`research-orchestrator` 的职责是：
1. 识别用户意图
2. 读取项目状态
3. 运行状态检查脚本
4. 根据状态机路由到对应 agent
5. 阻止非法动作

## Agent 路径解析规则

路由目标必须按以下优先级解析：

1. `.opencode/agents/<目标>/AGENTS.md`
2. `.opencode/agents/<目标>.md`

如果路由目标不存在，必须停止并报告路径不一致，禁止用 skill 或普通搜索替代。

## “开始/执行/继续/OK”消歧规则

用户说“开始/执行/继续/OK”时，不得只凭词面决定动作，必须结合最近一轮是否存在明确执行清单。

| 上下文 | 动作 |
|---|---|
| 最近一轮已有明确执行清单，且用户未新增条件 | 视为确认执行该清单 |
| 最近一轮只是方向讨论或原则讨论，没有执行清单 | 进入规划/审计，不得直接修改文件 |
| 涉及修改 agent/skill/核心脚本 | 必须先有执行清单，用户确认后才允许修改 |
| 用户明确说“开始修改/按清单改/实施文件改动” | 可执行已确认清单 |

执行清单必须包含：修改哪些文件、修改目标、不修改什么、验证方式、回滚/停止条件。

## 消息路由

| 用户指令 | 路由目标 |
|---------|---------|
| `列出项目` / `全局看板` | `.opencode/agents/项目管理员/AGENTS.md` |
| `快速调研 X` / `简单调研 X` / `简明结论 X` | 快速调研模式：主 agent 直接加载「快速调研师」skill，输出报告（不建项目/不写知识包；默认不落盘，用户明确指令存档时写入 `快速调研目录/` 相对目录） |
| `进入 X` / `继续 X` / `执行` / `继续` | 状态检查 → PLAN_REVIEW 等确认 / approved 后 `.opencode/agents/调研员/AGENTS.md` 批次执行 |
| `调研 X` / `新建 X` / `新建调研 X` | `.opencode/agents/项目管理员/AGENTS.md` → PLAN_REVIEW → 等待用户确认 |
| `整理知识` / `整理一下` | `.opencode/agents/知识管理员/AGENTS.md`（手动触发全面审计） |
| `出报告` / `生成报告` | 报告闸门 → `.opencode/agents/报告/AGENTS.md` |
| `分析` / `问答` / `设计` / `方案` | `.opencode/agents/分析/AGENTS.md` |

## 推荐命令入口

优先使用以下命令降低歧义：

| 命令 | 用途 |
|---|---|
| `/新建调研 <主题>` | 创建合规调研项目并进入方案确认 |
| `/继续调研 <项目名>` | 检查状态；方案已确认后按调研员批次模式继续 |
| `/检查调研 <项目名>` | 只检查状态，不执行 |
| `/生成报告 <项目名>` | 证据链满足后生成报告 |
| `/整理知识 <项目名>` | 手动触发知识管理员全面审计 |

## Agent 注册表

| Agent | 路径 | 职责 |
|-------|------|------|
| research-orchestrator | `.opencode/agents/research-orchestrator.md` | 默认入口、路由、状态机、闸门、批次调度 |
| 项目管理员 | `.opencode/agents/项目管理员/AGENTS.md` | 骨架创建/修复+前置准备+PLAN_REVIEW |
| 调研员 | `.opencode/agents/调研员/AGENTS.md` | 采集+分析+合成提炼（DISCOVER→EXTRACT→SYNTHESIZE） |
| 知识管理员 | `.opencode/agents/知识管理员/AGENTS.md` | 知识体系维护（consolidation run） |
| 报告 | `.opencode/agents/报告/AGENTS.md` | 读取L0视图+SQLite，按 preset 组装 Markdown → 调 html-report |
| 分析员 | `.opencode/agents/分析/AGENTS.md` | AgenticRAG检索+问答/设计/方案 |
| 图片分析 | `.opencode/agents/图片分析/AGENTS.md` | 多模态图片分析（subagent） |

## Skill 注册表（按阶段授权）

| Skill | 用途 | 使用阶段 |
|-------|------|---------|
| 多源搜索 | 搜索编排 | 调研员采集阶段；全局 skill 例外 |
| 快速调研师 | 快速摸底调研（多视角+agentic检索+强制引用） | 快速调研模式（主 agent 直接执行）；全局 skill 例外 |
| qmd | 本地文档搜索 | 项目管理员/调研员/分析员 |
| html-report | HTML 渲染 | 仅报告 agent |
| super-slide | 幻灯片 | 仅幻灯片任务 |
| 去AI味儿 | 文本润色 | 报告/文本润色阶段 |
| opendataloader-pdf | PDF 提取 | 采集/分析阶段按需 |

禁止使用未注册的外部流程、旧模板或临时笔记流程替代本平台状态机。

## 平台脚本调用规则（全局硬约束）

所有 agent 在调用 `.opencode/scripts/` 下的脚本时，必须遵守：

1. **直接用完整路径运行**：`python .opencode/scripts/脚本名.py --project-path "..."`。路径已明确给出，不需要确认文件是否存在。
2. **禁止用 glob/grep 搜索脚本文件名**：`.opencode/scripts/` 是平台级目录，不在项目目录内，glob 搜不到。不要用 `glob("**/knowledge-search.py")` 之类的搜索——搜不到不代表脚本不存在，只代表你在错误的目录下搜。
3. **禁止因"找不到脚本"而绕过脚本自己写 SQL**：脚本存在且可用。如果运行报错，读报错信息排查（路径、编码、schema）；不要回退到直接操作 SQLite。
4. **禁止用 Read 工具打开脚本源码来"理解实现"**：脚本的接口（命令行参数+输出格式）在 agent 文件中已完整描述。按描述使用即可，不需要读源码。
5. **zhishibao skill 脚本例外**：zhishibao 知识包相关脚本（`knowledge-search.py` / `generate-knowledge-views.py` / `build-relations.py` / `embed-claims.py`）已统一到 `.opencode/skills/zhishibao/scripts/`，**不在 `.opencode/scripts/`**。调用时用 `python .opencode/skills/zhishibao/scripts/脚本名.py --project-path "..."`。其余平台脚本（check-research-state / merge-batch / checkpoint 等）仍在 `.opencode/scripts/`。
6. **PowerShell 调脚本传 JSON 规范**：见全局 `~/.config/opencode/AGENTS.md` "PowerShell 调脚本传 JSON 规范"段。要点：含中文/嵌套 JSON 参数优先用 `--xxx-file 临时文件`，避免内联 `--xxx '{...}'` 被 PowerShell argparse 截断；必须内联用单引号+反引号转义双引号；中文 JSON 文件 UTF-8 无 BOM；终端乱码运行 `chcp 65001`。

## 面向人类表述规范（全局硬约束）

> 调研技术密度高、堆叠密集，人类难以阅读中间信息和内部编号。**区分执行通道与面向人类通道**。

**执行通道（agent 间/脚本调用/知识包写入）**：保留内部 ID（CL/S/A/C/D/LD/DB），机器可追溯、可关联、可入库。**这部分不变**。

**面向人类通道（报告/反馈/陈述/对话中间叙述）**：内部 ID 必须翻译为"业务性表述"——人类能直接读懂的引用方式：

| 场景 | ❌ 内部编号（禁） | ✅ 业务性表述（允） |
|---|---|---|
| 报告正文 | "CL03875 表明..."、"采录-S0578"、"分析-A062" | "在《运维场景知识服务体系对比》中的'分层知识体系'观点"（用 display_citation 反查） |
| 反馈/陈述 | "S0578 提出..."、"CL00564 取代 CL00561" | "在《知识萃取规范指南》中的XX主张" / "新数据完整版修正了原版据疑张冠李戴的数值" |
| 对话中间叙述 | "我们查到 CL00913…" | "我们查到一条关于'经验如何升级为能力'主张（采自 S0142）…" |

**阈值分级**：
- **报告正文**：完全无内部 ID（报告员已有规则，见 `报告/AGENTS.md`"禁止外显"段）
- **反馈/陈述/对话叙述**：业务性表述为主，内部 ID 只能作为括注溯源括号出现（例："在《XX》中的主张（CL03875）"），且必须伴随业务性主语、禁止独立成段、单次输出括注不超过 3 处——不丢追溯性，但主语必须是业务表述，不能是 ID
- **执行日志/.task 记录/知识包写入**：保留内部 ID，不翻译

**反查入口**：用 zhishibao skill `--action source --source-id Sxxxx` 查来源的 display_citation/标题/URL，再业务化表述。无法反查的内部 ID 在面向人类通道**不得外显**。

**关键原则**：内部 ID 是机器资产不是人类语言；面向人类时把它们翻译成"在什么源里的什么观点"是 agent 的义务，不是可选润色。

## sub agent 启动协议（全局硬约束）

> sub agent（通过 task 工具启动）每次是**全新上下文**，只有 task prompt 里的内容。主 agent/上级 agent 调 sub agent 时，task prompt 必须含三段，缺一不可：

1. **项目目标锚点**（一句话，来自 `project.config.md#objectives.problem` + `audience`）--让 sub agent 知道自己在为什么贡献。例："本项目目标是构建服务于 AI agent 运维场景的知识体系，首要服务对象是 agent"
2. **本任务为什么做**（与项目目标的关联 + 上游已做了什么 + 本任务在链路中的位置）--例："上游已仲裁 30 对冲突，本任务是治理 471 条无边界断言以提升知识包可信度，为后续报告闸门做准备"
3. **任务范围/产出/硬约束/输出格式**--具体做什么、产出什么、不碰什么、怎么返回

**禁止只给"任务范围/产出"而不给"为什么/目标锚点"**：sub agent 缺目标锚点会局部最优、就事论事偏离项目真实意图（如仲裁理由就事论事而不锚定"知识服务体系"目标）。

**例外**：纯机械子任务（如"对这 5 个文件做 OCR"）可省略第 1 段，但第 2 段（为什么做）仍必填。

## 知识检索规则（全局硬约束）

所有 agent 在检索知识包内容时，必须遵守：

1. **知识检索（发现知识）必须通过 knowledge-search.py**：不要用 glob 搜索 raw-S*.md / 采录-S*.md / 分析-A*.md 等数据文件然后逐个 Read 来"发现"知识。不要用 grep 在数据文件中搜索关键词来替代 `--action search/hybrid`。不要直接用 Python+sqlite3 写 SQL 替代 knowledge-search.py。
2. **Read 数据文件是合法的——但前提是"定位后深读"**：knowledge-search.py 返回断言ID和source ID后，需要深读该来源的完整证据时，用 Read 打开具体文件。这是"定位后深读"，不是"搜索发现"。
3. **以下文件随时可 Read，不受限制**：L0/L1 视图、project.config.md、task_queue.md、.task/ 下产出文件等配置/视图/过程文件。
4. **为什么检索必须走脚本**：自己 glob+grep 会绕过向量语义检索（弥合词汇差异）、关系跳转（发现关联断言）、概念反向索引等能力，导致检索不完整。

## 知识底座原则

本平台不是报告生成器。调研的核心目标是建立可持续生成、校验、复用和再派生观点的知识系统；报告、方案、问答和设计只是知识系统的一次消费。

知识沉淀不是把观点写进知识包，而是把可追溯、可复用、可更新、可反驳的知识单元写入知识包。任何知识包条目都应尽量连接证据、边界、反例、适用场景、可回答问题和未决问题。

## 调研与分析的职责边界（全局硬约束）

> **调研阶段如实记录不评判不生产观点；分析员按需形成输出性观点。**

- **调研阶段（调研员/采录员/A*/C*）**：调研"人家说了什么"，如实记录原文与主张；消化理解他人的知识；记录"这一家观点与那家观点有什么冲突"（客观陈述对立）。**不评判（不判谁对谁错）、不生产观点（不产出立场性结论）**。A*/C* 是客观证据台账，不是评判报告。
- **分析阶段（分析员）**：根据知识包+用户需求+场景，形成**输出性观点**（知识推理和派生）--立场/判断/推荐在此产生。输出性观点必须可追溯到知识包断言，不臆测。
- **边界不可越**：调研员不得在 A*/C* 中产出"本调研认为/我们建议"类立场；分析员不得在调研阶段介入（分析员是知识消费方，不是调研生产方）。

标准流程：意图理解/需求深化 → 自主调研规划 → 观点/概念收集 → 证据化采录 → 聚合/对冲 → 观点派生 → 知识包沉淀 → 知识应用 → 应用反馈 → 回流调研队列。

## 调研策略层

本平台采用三层轻量策略模型：

```yaml
research_type: 决定最终产物
research_subtype: 描述具体任务
strategy_tags: 决定怎么挖、怎么评估来源
```

调研员各阶段必须按需读取 `.opencode/research-strategies/`。社区反馈、电商评价、售后投诉等来源策略可先写入 agent 原则，具体 skill 抓取能力后续补齐。

## 调研项目启动闸门

调研员启动前，项目目录必须包含：

- `project.config.md`
- `0-规划/` 或 `1-规划/`
- `task_queue.md`
- `01-采集记录/` 或 `2-执行/01-采集记录/`
- `02-分析提取/` 或 `2-执行/02-分析提取/`
- `03-知识提炼/` 或 `2-执行/03-知识提炼/`
- `04-检查/` 或 `2-执行/05-过程产物/`

缺任一项，不得进入调研；必须回到项目管理员 agent 修复骨架。

## 状态检查脚本

调研相关动作前必须运行或等价执行：

```powershell
.opencode/scripts/check-research-state.py --project-path "<项目路径>"
```

根据返回的 `next_stage` 路由：

| next_stage | 允许动作 |
|---|---|
| INVALID_PROJECT | 停止或调用项目管理员 agent 修复 |
| PLAN_REVIEW | 输出调研方案草案，等待用户确认，禁止采集 |
| DISCOVER | 调研员批次执行 |
| EXTRACT | 调研员补采录 |
| SYNTHESIZE | 调研员分析提取 |
| KNOWLEDGE_PACK | 知识管理员consolidation |
| CLOSED_LOOP | 知识管理员饱和判定 |
| REPORT_ALLOWED | 允许报告 agent |

## 调研产物不可替代规则

以下替代关系全部无效：

- 搜索摘要 ≠ `raw-S*.md`
- `.task/findings.md` ≠ 采录
- `progress.md` ≠ 分析
- 报告/实施指南 ≠ 知识包
- 手写总结 ≠ SYNTHESIZE

## project.config 边界规则

`project.config.md` 只承载**静态项目定义**（PLAN_REVIEW 后冻结）：元信息、调研目标（objectives 四问）、调研工艺/流程、策略映射、知识包 Schema、ID 注册表。

运行时累积信息一律写入 `run-state.md`，禁止写入 `project.config.md`：
- 批次进度、已有采录数、当前批次编号、模型配比、批结论摘要
- `explore_keywords`/`focus_keywords`（调研员每批次累加）
- `dead_ends` 表
- 用户售前定义、阶段性意见、运行期临时定义

违反此边界 = 项目定义被运行态污染（如本案项目 117 行 config 中运行态占 95 行）。批次结论/用户阶段性意见一律写入 `run-state.md` 或 `.task/`。

## 状态标记证据规则

任何 `complete` 状态必须有对应文件证据：

| 状态 | 必须存在 |
|---|---|
| DISCOVER complete | `raw-S*.md` |
| EXTRACT complete | `采集记录-S*.md` 或 `采录-S*.md` |
| SYNTHESIZE complete | `分析-A*.md` / `对比-C*.md` / `链-C*.md` |
| 知识包 complete | `knowledge-pack*.json` 非空 |
| 报告 complete | 报告文件 + 上述证据链完整 |

## 报告生成闸门

报告/实施指南/总结类产物只有在以下条件满足时允许生成：

- `raw-S*.md >= 3`
- `采集记录-S*.md` 或 `采录-S*.md >= 3`
- `分析-A*.md` 或 `对比-C*.md` 或 `链-C*.md >= 1`
- `knowledge-pack*.json` 或 `knowledge-index.db` 存在且非空
- **知识一致性检查通过**（无contested断言、无stale对比文档）

### 严重度梯度（三级，非全有或全无）

闸门检查项按严重度分级处理，不是"任一不满足就全阻断"：

| 级别 | 含义 | 处理 | 示例 |
|---|---|---|---|
| **error** | 阻断性缺陷，不满足不准出报告 | 阻断，只输出缺口 | raw<3 / 采录<3 / 知识包空 / 有未仲裁contested |
| **warning** | 非阻断性质量风险，标注后可出报告 | 报告附"质量警告"段 | 单源结论占比高 / embedding缺口大 / 孤儿断言多 |
| **info** | 提示性信息，不影响出报告 | 报告附"说明"段 | 线索池仍有pending / 部分来源时效偏旧 |

error 项不满足时只能输出证据链缺口，禁止写报告；warning/info 项标注后允许出报告。

### 诚实边界声明

> **lint 检查（闸门+health+验收脚本）保证结构完整性和一致性，不保证事实准确性。**
> 闸门验证的是"证据链是否完整、知识包是否一致、冲突是否仲裁"，不验证"断言内容是否真实正确"。事实准确性由来源可信度+多源交叉验证+分析员判断共同保障，不是闸门能保证的。报告不得宣称"已通过质量验证"暗示事实准确，应如实表述"已通过结构一致性检查"。

## 调研员子阶段

定义在 `.opencode/agents/调研员/02-` ~ `04-` 文件中（01-前置准备已移至项目管理员，05/06职责已移交知识管理员）：

1. 采集 → 搜索 + 抓取 + 取证（02-采集.md）
2. 分析提取 → 单源分析 + 跨源对比（03-分析提取.md）
3. 质量闸门 → 追溯 + 检查 + 终验（04-质量闸门.md）

知识体系维护（索引/冲突检测/视图/饱和判定）由知识管理员负责，定义在 `.opencode/agents/知识管理员/AGENTS.md`。

## 核心原则

设计 agent 不能让它靠猜，必须提供清晰约束。约束应下沉到：

- `opencode.json` 默认入口
- primary agent 路由
- agent 权限管理
- command 固定入口
- 状态检查脚本
- 文件产物闸门
