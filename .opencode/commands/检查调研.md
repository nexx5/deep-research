---
description: 只检查调研项目状态，不执行采集、分析或报告
agent: research-orchestrator
---
用户请求检查调研：$ARGUMENTS

严格执行：
1. 定位项目目录。
2. 运行 .opencode/scripts/check-research-state.py。
3. 输出项目骨架、plan_status、approval_required、execution_mode、execution_status、research_type、raw数量、采录数量、分析数量、知识包状态、next_stage、report_allowed、blocking_reasons。
4. 不调用调研员，不调用报告agent，不修改项目内容。
