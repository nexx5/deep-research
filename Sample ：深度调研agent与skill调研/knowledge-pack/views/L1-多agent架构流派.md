# L1 主题视图：多agent架构流派

> 自动生成于 2026-08-04 17:47 | 关联断言 0 条
>
> 这是按需生成的深入视图。如需更完整信息，请直接检索 SQLite。

---

未找到与「多agent架构流派」相关的断言。

建议：
1. 用 `knowledge-search.py --action search --query "多agent架构流派"` 做全文检索
2. 用 `knowledge-search.py --action vector --query "{topic}"` 做语义检索
3. 检查概念标签是否匹配